#include <iostream>
using namespace std;

#define REP(i,a,b) for (int i = a; i < b; ++i)

int main(){
    int n, m;
    cin >> n;
    int nums[n];
    REP(i,0,n){
        cin >> nums[i];
    }
    cin >> m;
    int nums[m];
    REP(i,0,m){
        cin >> que[i];
    }
}
